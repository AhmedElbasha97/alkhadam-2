import 'package:flutter/material.dart';

class AppColor {
  // ── Primary brand — Rosana Resort Blue ─────────────────────────────────────
  static const Color lightPrimaryColor    = Color(0xFF008CFF);
  static const Color primaryDark          = Color(0xFF0070D6);
  static const Color primaryDeep          = Color(0xFF003A70);
  static const Color primaryLight         = Color(0xFFE3EEFF);

  // ── Neutral / Background ────────────────────────────────────────────────────
  static const Color lightSecondaryColor  = Color(0xFF141411);
  static const Color lightBackGroundColor = Color(0xFFF0F6FF);
  static const Color lightGreyColor       = Color(0xFF4A6080);
  static const Color whiteTextColor       = Color(0xFFFFFFFF);
  static const Color darkBlueColor        = Color(0xFF003A70);

  // ── Cards / Fields / Borders ────────────────────────────────────────────────
  static const Color cardColor            = Color(0xFFD4E6FF);
  static const Color fieldColor           = Color(0xFFE3EEFF);
  static const Color borderColor          = Color(0xFF90C4FF);
  static const Color dividerColor         = Color(0xFFB3D4FF);
  static const Color stepInactiveColor    = Color(0xFF7AAAC8);

  // ── Semantic ────────────────────────────────────────────────────────────────
  static const Color errorColor           = Color(0xFFD32F2F);
  static const Color successColor         = Color(0xFF00C47A);
  static const Color blueColor            = Color(0xFF008CFF);
}
